import { DefaultFieldValuePipe } from './default-field-value.pipe';

describe('DefaultFieldValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultFieldValuePipe();
    expect(pipe).toBeTruthy();
  });
});
